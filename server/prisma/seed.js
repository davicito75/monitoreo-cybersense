// This file is a compiled artifact of prisma/seed.ts and has been intentionally emptied.
// Keep the source at `seed.ts`. You can run the seed using `pnpm --filter server run seed`.

console.log('seed.js intentionally cleared');
